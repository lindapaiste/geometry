/**
 * makes sure that a rectangle point will not cause a rectangle to overflow the boundary
 *
 * for corners, this means that the corner is inside the boundary
 *
 * for midpoints, it means the midpoint won't cause a corner to overflow
 */
export default class RectanglePointBoundary {

    //TODO
}
